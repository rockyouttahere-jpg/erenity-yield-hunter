import { SchemeNetworkServer, Price, Network, AssetAmount, PaymentRequirements } from '@okxweb3/x402-core/types';

/**
 * EVM server implementation for the aggr_deferred payment scheme.
 *
 * Parses prices the same way as ExactEvmScheme. The OKX Facilitator batches
 * session key signatures on-chain asynchronously; the Seller receives
 * status=success immediately and delivers the resource without polling.
 */
declare class AggrDeferredEvmScheme implements SchemeNetworkServer {
    readonly scheme = "aggr_deferred";
    parsePrice(price: Price, network: Network): Promise<AssetAmount>;
    enhancePaymentRequirements(paymentRequirements: PaymentRequirements, supportedKind: {
        x402Version: number;
        scheme: string;
        network: Network;
        extra?: Record<string, unknown>;
    }, extensionKeys: string[]): Promise<PaymentRequirements>;
    private parseMoneyToDecimal;
    private defaultMoneyConversion;
    private convertToTokenAmount;
}

export { AggrDeferredEvmScheme };
